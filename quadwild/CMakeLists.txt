execute_process(
    COMMAND qmake ${CMAKE_CURRENT_SOURCE_DIR}/quadwild.pro
    WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}
    COMMAND_ECHO STDOUT
    OUTPUT_VARIABLE outVar
)

execute_process(
    COMMAND make
    WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}
    COMMAND_ECHO STDOUT
    OUTPUT_VARIABLE outVar
)
